# Proposed steps: gov.sg — Homepage Load (Smoke Test)

## Proposed steps

1. Navigate to `https://www.gov.sg` and wait for `load`.
2. Assert page title matches `/home \| gov\.sg/i`.
3. Assert the "A Singapore Government Agency Website" identity banner is visible — present on all .gov.sg pages, reliable stable assertion.
4. Assert at least one nav link (`Explainers`) is visible in the top nav.
5. Assert the footer contains `"Government of Singapore"`.
